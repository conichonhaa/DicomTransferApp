<Window x:Class="MammoListeApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Liste des Mammographies de Dépistage"
        Height="680" Width="1150"
        MinHeight="500" MinWidth="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock
            Grid.Row="0"
            Text="Liste des Mammographies de Dépistage"
            FontSize="20"
            FontWeight="Bold"
            HorizontalAlignment="Center"
            Margin="0,0,0,14"/>

        <!-- Barre de contrôles -->
        <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="4" Padding="10,8" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal">
                <!-- Date -->
                <TextBlock Text="Date :" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0"/>
                <DatePicker x:Name="dpDate" Width="140" VerticalAlignment="Center" Margin="0,0,20,0"/>

                <!-- Salle -->
                <TextBlock Text="Salle :" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,6,0"/>
                <ComboBox x:Name="cboSalle" Width="170" VerticalAlignment="Center" Margin="0,0,20,0">
                    <ComboBoxItem Content="Toutes les salles" Tag="" IsSelected="True"/>
                    <ComboBoxItem Content="ZK" Tag="ZKPRISTINA"/>
                    <ComboBoxItem Content="HK" Tag="HKPRISTINA"/>
                    <ComboBoxItem Content="Cloche d'Or" Tag="MCCO_MG1"/>
                </ComboBox>

                <!-- Filtre dépistage -->
                <CheckBox x:Name="chkDepistageSeul" Content="Dépistage uniquement" VerticalAlignment="Center" IsChecked="True" Margin="0,0,20,0"/>

                <!-- Boutons -->
                <Button x:Name="btnRechercher" Content="🔍  Rechercher" Padding="12,6" Margin="0,0,10,0" FontSize="13"/>
                <Button x:Name="btnExportExcel" Content="📊  Exporter Excel" Padding="12,6" Background="#E3F2FD" FontSize="13"/>
                <Button x:Name="btnConfig" Content="⚙ Config PACS" Padding="8,6" Margin="10,0,0,0" FontSize="12" Foreground="#555"/>
            </StackPanel>
        </Border>

        <!-- Statut -->
        <TextBlock x:Name="txtStatus" Grid.Row="2" Margin="2,0,0,6" Foreground="#555" FontStyle="Italic"/>

        <!-- Tableau des résultats -->
        <DataGrid
            x:Name="dgResultats"
            Grid.Row="3"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            GridLinesVisibility="Horizontal"
            AlternatingRowBackground="#F9F9F9"
            RowHeight="26"
            HeadersVisibility="Column">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#1565C0"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="8,4"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Salle" Binding="{Binding Salle}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="4,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Patient" Binding="{Binding NomPatient}" Width="180">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="4,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Patient ID / Matricule" Binding="{Binding PatientID}" Width="140">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontFamily" Value="Consolas"/>
                            <Setter Property="Padding" Value="4,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Date Naissance" Binding="{Binding DateNaissanceFormatee}" Width="110">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Heure" Binding="{Binding HeureFormatee}" Width="70">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Padding" Value="4,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="AE Source" Binding="{Binding SourceAETitle}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="Gray"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="Padding" Value="4,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Journal -->
        <TextBox
            x:Name="txtLog"
            Grid.Row="4"
            Height="90"
            Margin="0,8,0,0"
            IsReadOnly="True"
            TextWrapping="Wrap"
            VerticalScrollBarVisibility="Auto"
            FontFamily="Consolas"
            FontSize="10"
            Background="#F5F5F5"
            Foreground="#333"/>
    </Grid>
</Window>
